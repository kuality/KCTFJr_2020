service misc
{
    disable = no
    type        = UNLISTED
    wait        = no
    server      = /home/misc/run.sh
    socket_type = stream
    protocol    = tcp
    user        = misc
    port        = 12345
    flags       = REUSE
}
